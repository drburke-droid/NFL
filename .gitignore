__pycache__/
*.pyc
.claude/

# ESPN league data syncs via git (user opted in 2026-07-02 — private repo; cookies expire ~yearly).
# The GitHub Action still uses ESPN_S2/ESPN_SWID repo Secrets independently.
