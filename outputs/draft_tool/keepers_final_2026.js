// FINAL 2026 keeper selections — NOT YET CONFIRMED.
// Confirm each team's actual keepers in the 🧩 Keepers tab, tick ✓ verified on each
// card, then hit 🔒 LOCK as FINAL. The lock is saved in the browser (localStorage
// "keepers_final_lock") and re-applied on every page load — "Clear all", resets and
// mock drafts can't erase it; only 🔓 Unlock can.
// To make the lock survive a wiped browser / other devices too: after locking, use
// ⬇ Export finals (or ask Claude) and replace this file with the exported one.
const KEEPERS_FINAL = null;
